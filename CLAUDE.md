# Claude Code guidance

Use `/fullstack-forge` for orchestration and `/forge-<section>` for a specialist audit. Canonical
source lives under `src/fullstack-forge/`; `.claude/skills/` is generated. Follow `AGENTS.md`, keep
audits read-only, require approval for risky changes, and report `NOT_VERIFIED` instead of inventing
runtime evidence.
