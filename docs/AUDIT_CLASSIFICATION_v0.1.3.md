# Independent corrective audit — classification sweep (v0.1.3)

Baseline commit: `b22f89d280ca296f32ebd783fe0ff708e772cf28` (`origin/main`, tag `v0.1.2`)
Baseline suite: `npm test` → 117 pass / 0 fail, exit 0
Branch: `fix/v0.1.3-correctness-audit`

Every classification below is derived from direct source inspection or an executed reproduction,
not from the report text supplied with the task.

| Issue | Classification | Reproduction / source evidence | Dependencies | Planned resolution | Regression test |
| --- | --- | --- | --- | --- | --- |
| §2 Fix refusal overwrites defect status | CONFIRMED | `cli/src/fixes.ts:551` unconditionally assigns `finding.status = "BLOCKED"` on refusal, destroying the original `FAIL`/`WARNING`. No fix-attempt structure exists on `Finding` (`cli/src/types.ts:50-68`). | none | Add `fix_attempts[]` to the finding schema; preserve defect status; record fix-attempt status separately. | `blocked safe fix preserves original FAIL` / `... WARNING` |
| §3 `--safe` is a no-op | CONFIRMED | `options.safe` is initialised (`cli.ts:466`), assigned (`cli.ts:488`), declared (`types.ts:141`) and **never read** anywhere in `cli/src`. `cli.ts:548` documents it as functional. | §2 | Enforce contract: `fix` plans only; `fix --safe` executes safe registry entries only. | `safe flag contract`, `fix without --safe performs no write` |
| §4a Application gates disabled | CONFIRMED | `gates.ts:96` marks every `category: "internal"` command gate `NOT_APPLICABLE`, `required: false` for non-Forge repos. That covers `FF-GATE-SECRETS`, `FF-GATE-DEPENDENCIES`, `FF-GATE-LICENSES`. | none | Split registry into `forge-self` / `audited-application` / `project-native` classes. | `application-side dependency gate`, `application-side secret gate`, `Forge self-release gate separation` |
| §4b Unreachable dependency fallback | CONFIRMED | `FF-GATE-DEPENDENCIES` declares `command: "audit:dependencies"` (`gates.ts:42`), so it always enters the `definition.command !== undefined` branch at `gates.ts:95` and `continue`s. `gates.ts:112-130` is dead code. | §4a | Remove the dead branch; make dependency evidence a first-class application gate. | covered by `application-side dependency gate` |
| §5 Analyzer data-flow depth | CONFIRMED | `analyzers.ts:1470 containsRequestData()` is a raw-text regex on `req.(body\|params\|query\|...)`; aliases are invisible. `analyzers.ts:1515 hasValidationNear()` regex-scans preceding text of the enclosing function, so unrelated `validate*` calls suppress real findings. | §6 | Bounded intra-file taint engine: aliases, reassignment, destructuring, template/concat propagation, same-file parameter summaries; sanitizer bound to the tainted symbol. | alias SQL/shell, destructured SSRF, parameter propagation, unrelated-validation fixtures |
| §6 Findings are rule-global, not instance-specific | CONFIRMED | `finding.ts:23` enforces `^FF-[A-Z0-9-]+-[0-9]{3,}$` only. `mergeIssues()` (`analyzers.ts:1215`) collapses all occurrences of a rule into one finding. No instance identity exists. | none | Add stable `instance_id = <rule>:<hash(path, sink, structure)>`, backwards compatible. | `instance-specific findings`, `instance-specific verification` |
| §7a Verification runs analyzers globally | CONFIRMED | `verification.ts:81` calls `runNamedAnalyzer(id, root)` over the whole repo, then matches `candidate.id === action.finding_id` (`:82`). Any unrelated occurrence of the same rule re-fails a resolved finding. | §6 | Scope verification to the instance and its evidence paths. | `instance-specific verification` |
| §7b `verify --dry-run --allow-run` executes commands | CONFIRMED | `options.dryRun` is consulted only at `verification.ts:68` for report writing. `executeAction` reaches `runFile(...)` at `:135` whenever `allowRun` is true, regardless of dry-run. | none | Thread `dryRun` into `executeAction`; plan-only, no execution. | `verify dry run executes no commands` (sentinel file) |
| §8a Changed-scope base falls back to `HEAD` | CONFIRMED | `scope.ts:197` returns `"HEAD"` as the final fallback, making `merge-base HEAD HEAD == HEAD` and hiding all committed branch changes. Upstream (`@{upstream}`) and `origin/main`/`origin/master` are never consulted (`scope.ts:185-198`). | none | Implement the specified precedence and return `BLOCKED` instead of `HEAD`. | `changed-scope upstream base`, `changed-scope missing base blocks` |
| §8b Unbounded impact expansion | PARTIALLY_CONFIRMED | `expandGraph()` traverses forward and reverse import graphs; `assertImpactBudget` (`scope.ts:115`) does bound total size and fails closed, so it does not "crash or imply full coverage". Expansion is nonetheless undifferentiated and workspace expansion is whole-directory. | §8a | Add depth-bounded, reason-classified expansion; keep the existing budget failure mode. | `changed-scope bounded expansion` |
| §9a Every nested manifest is a workspace/application | CONFIRMED | `discovery.ts:532-541` maps every non-root `package.json` to a `package-workspace` at `confidence: "HIGH"` and every manifest to an application, ignoring declared workspace config. | none | Honour declared workspaces; downgrade undeclared manifests. | `declared workspace detection` |
| §9b Repository confidence check is dead | CONFIRMED | `.git` is in `EXCLUDED` (`discovery.ts:21`), so `relativeFiles.some(p => p.startsWith(".git/"))` (`discovery.ts:529`) is always false and confidence is permanently `MEDIUM`. | none | Use `git rev-parse --is-inside-work-tree`. | `repository confidence uses git inspection` |
| §9c Route detection is Express-literal only | CONFIRMED | No framework route adapters exist in `discovery.ts`; visibility is inferred from a nearby text window. | §9a | Add bounded adapters (Express, Fastify, Next App/Pages, NestJS, Hono) with declared confidence. | `Next.js route detection`, `NestJS route detection`, `Fastify route detection`, `framework middleware visibility` |
| §10 Missing-adapter reporting is generic | CONFIRMED | No analyzer support registry exists anywhere in `cli/src` (grep for `adapter` returns only an unrelated string at `analyzers.ts:341`). | §9c | Add a structured support registry; emit module/language/framework/required-adapter on gaps. | `structured missing-adapter reporting` |
| §11 Expand automated evaluations | PARTIALLY_CONFIRMED | Archive-extraction and scanner-boundary signals are genuinely absent; the remainder correctly require manual proof. | §5 | Add static signals for extraction limits and scanner boundary only; keep runtime resolution `NOT_VERIFIED`. | archive-extraction limit fixtures |
| §12 v0.1.2 verification record missing | CONFIRMED | `docs/` contains a v0.1.1 verification record; no `docs/RELEASE_VERIFICATION_v0.1.2.md` exists at `b22f89d`. | none | Document the gap accurately; do not backdate. Produce a v0.1.3 record. | `current release verification` |

## Not reproduced

None of the reported hypotheses were rejected outright. §8b and §11 are narrowed as stated above.
