# Fullstack Forge v0.2.1 release verification

Verification stage: CANDIDATE_LOCAL

Local validation status: PASS

Remote publication status: PENDING

This record describes the locally validated v0.2.1 candidate intended for tagged source. It does not
claim a GitHub Release, remote CI result, attestation, or published asset before observation.

## Required local evidence

- [x] vendored provider pins, checksums, paths, file caps, licences, notices, authority transforms,
      foreign-install denial, runtime reachability, and generated attribution validated; the
      authoritative file count is generated by `npm run upstream:verify`, not duplicated here
- [x] exact composition matching, nested condition grammar, explicit-request precedence, saturated
      budgets, Sentry stack routing, production runtime integration, and report ledger tested
- [x] complete platform archives extracted and validated in clean temporary directories
- [x] fresh, update, stale-file, uninstall, smoke, and offline installation behavior tested,
      including a public-archive v0.1.0-to-v0.2.1 upgrade; modified stale bytes are preserved but
      obsolete ownership is removed, and offline doctor passes afterward
- [x] formatting, lint, typecheck, test, coverage, validation, packaging, licence, workflow,
      tagged-document, traceability, branding, and secret checks completed locally
- [x] npm payload inspected; source-only maintainer network/update scripts are not shipped, while
      the retained offline `upstream:verify` command passes against path-bound runtime checksums in
      a clean packed installation without the pristine `third_party/` tree
- [ ] exact-clean-main `forge ship --allow-run --json` on the verified merge commit

## Local runtime matrix

All three supported Node lines ran the real `npm run test:coverage` command on Windows:

| Runtime      | Result | Tests                  | Lines  | Branches | Functions |
| ------------ | ------ | ---------------------- | ------ | -------- | --------- |
| Node 20.19.5 | PASS   | 1,128 pass / 1 skipped | 94.42% | 85.56%   | 95.04%    |
| Node 22.21.1 | PASS   | 1,128 pass / 1 skipped | 94.41% | 85.51%   | 95.04%    |
| Node 24.14.1 | PASS   | 1,128 pass / 1 skipped | 91.51% | 82.58%   | 91.48%    |

The single skip is the symlink-hostility fixture because this Windows account cannot create a
symlink. The test reports the missing operating-system capability rather than treating it as proof.

The network-backed upgrade installed the exact public v0.1.0 tag archive, initialized all hosts and
a separate Codex-only installation, upgraded both to the candidate, passed offline doctor after the
adversarial stale-file migration, retired unchanged stale, renamed-module, removed-provider, and
obsolete ownership records, preserved modified stale and user-created files, kept bare update and
uninstall scoped to installed hosts, and left zero symlinks. Exact-head CI and the tag workflow both
run this genuine upgrade rather than relying only on the development-preview fixture.

The complete local `npm run check` passed 1,129 tests (1,128 pass, 1 capability-based skip), then
validated 46 canonical skills, all six generated host roots, nine archives with 9,777 aggregate
entries and 644 adapter resolutions, 91 package dependencies, eight vendored providers, three
workflows, 87 traceability requirements, and 2,815 secret-scanned files with zero findings.

Composition evaluation passed 40/40 cases, including 13/13 saturated-budget cases and 134 expected
suppressions. Median eager context growth is +101.7% and the maximum is +475.9%; median available
growth is +282.3% and the maximum is +1034.9%. These measured figures do not meet the context
efficiency target and are not presented as doing so.

## Required remote evidence

- [ ] exact-head CI on Ubuntu, Windows, and macOS with Node 20, 22, and 24
- [ ] exact-head dependency review and CodeQL
- [ ] annotated `v0.2.1` tag bound to the verified merge commit
- [ ] GitHub Release assets, checksums, attestations, immutability, and downloaded-byte verification
- [ ] README changed to call v0.2.1 current only after publication succeeds

All remote steps remain pending.

## Limitations

- Live agent-host activation is `NOT_VERIFIED`.
- Independent external-corpus precision remains `NOT_VERIFIED`.
- Linux and macOS execution, and the complete OS/Node matrix, remain `NOT_VERIFIED` until exact-head
  CI completes.
- Impeccable detector execution is not part of v0.2.1; reviewed guidance is translated through Forge
  contracts and subjective results cannot block Ship.
