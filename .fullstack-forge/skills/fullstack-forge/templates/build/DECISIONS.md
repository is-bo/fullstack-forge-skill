# Build decisions log

This file is append-only. Add a new entry below the last one for every framing or feature decision;
never delete or rewrite a prior entry. If a decision changes, add a new entry that says so and
supersedes the old one explicitly — do not edit history to make it look like the new decision was
always in place.

## <date> — <decision title>

- Scope: `project` or `feature:<slug>`
- Decision:
- Reason:
- Alternatives considered:
- Non-goal implied (if any), with reason:
- Revisit when:

Replace the placeholder above with the real first entry, then keep appending. Do not fabricate a
decision that was not actually made; an unresolved question is its own valid entry.
