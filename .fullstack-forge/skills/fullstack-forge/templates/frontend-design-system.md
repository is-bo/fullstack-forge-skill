# Frontend design-system decision record

Use this template only when creating or materially extending a shared system. Prefer the existing
project source of truth and record only decisions this change owns.

## Existing system

- Token source and theme mechanism:
- Component and styling libraries:
- Typography, icon, chart, and brand sources:
- Nearest precedents:

## Semantic tokens

- Color roles and light/dark mappings:
- Type roles and content measures:
- Spacing and density scale:
- Radius, border, elevation, and layer scale:
- Layout widths, grids, and breakpoint decisions:
- Motion roles and reduced-motion mapping:
- Icon and data-series roles:

## Components and states

- Primitives reused:
- New reusable contracts and justification:
- Variants:
- Default, hover/press, focus, selected, disabled, loading, empty, error, and success behavior:
- Responsive and localized behavior:

## Change control

- Public API or migration impact:
- Existing values deliberately preserved:
- Approval-required visual-language changes:
- Rendered, accessibility, and regression evidence:
