# Gemini CLI guidance

Activate `fullstack-forge` or a `forge-<section>` skill with `/skills`, then request `audit`, `fix`,
`verify`, or `report`. Canonical source lives under `src/fullstack-forge/`; `.gemini/skills/` is
generated. Follow `AGENTS.md`, preserve raw failures, and never treat unavailable evidence as a
pass.
